<% provide(:title, @page.title) %>
<div class="row">
  <aside class="span4">
    <section>
      <h1>
        <%= @page.title %>
      </h1>
    </section>
  </aside>
</div>

<%= nested_pages @page.root.subtree.arrange(:order => :id) %>